name: Build IPA

on:
  push:
    branches: [ master ]
  workflow_dispatch:  # allows manual trigger from the GitHub Actions UI

jobs:
  build:
    runs-on: macos-15  # Xcode 16.2, iOS 18 SDK

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Select Xcode 16.2
        run: sudo xcode-select -s /Applications/Xcode_16.2.app

      - name: Build (unsigned)
        run: |
          xcodebuild \
            -project TerrainMapper.xcodeproj \
            -scheme TerrainMapper \
            -configuration Release \
            -sdk iphoneos \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            ONLY_ACTIVE_ARCH=NO \
            build

      - name: Package as IPA
        run: |
          APP=$(find ~/Library/Developer/Xcode/DerivedData \
            -name "TerrainMapper.app" \
            -path "*/Release-iphoneos/*" | head -1)

          echo "Found app: $APP"
          ls "$APP/Info.plist"  # verify it's complete

          mkdir -p Payload
          cp -r "$APP" Payload/
          zip -r TerrainMapper.ipa Payload/
          echo "IPA size: $(du -sh TerrainMapper.ipa)"

      - name: Upload IPA as artifact
        uses: actions/upload-artifact@v4
        with:
          name: TerrainMapper-${{ github.run_number }}
          path: TerrainMapper.ipa
          retention-days: 30
